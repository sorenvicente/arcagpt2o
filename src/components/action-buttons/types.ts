export interface CustomButton {
  id: string;
  name: string;
  icon: string;
  label: string;
  category: string;
  color: string;
}

export interface MainButton {
  name: string;
  icon: string;
  label: string;
  category: string;
  color: string;
}