export const container = {
  center: true,
  padding: "2rem",
  screens: {
    "2xl": "1400px",
  },
};

export const borderRadius = {
  lg: "var(--radius)",
  md: "calc(var(--radius) - 2px)",
  sm: "calc(var(--radius) - 4px)",
};